# requirements.txt - SIMPLIFIED

# Core
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1

# LLM
google-generativeai==0.8.3

# HTTP client
httpx==0.27.2

# Storage
# (SQLite is built-in to Python)

# That's it! Much simpler.
# Removed: langgraph, notion-client, github, slack-sdk, etc.
# ReAct agent calls APIs directly via httpx